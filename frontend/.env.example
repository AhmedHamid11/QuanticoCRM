# API URL - set this to your backend URL in production
# For local development with vite proxy, use /api/v1
# For production, use full URL like https://your-api.up.railway.app/api/v1
PUBLIC_API_URL=/api/v1
