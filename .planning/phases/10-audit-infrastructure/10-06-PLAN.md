---
phase: 10-audit-infrastructure
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/security.yml
autonomous: true

must_haves:
  truths:
    - "gosec runs on push to main and PRs"
    - "gosec fails build on high-severity findings"
    - "govulncheck runs for dependency vulnerabilities"
    - "Results uploaded to GitHub security dashboard (SARIF)"
  artifacts:
    - path: ".github/workflows/security.yml"
      provides: "CI security scanning workflow"
      contains: "gosec"
  key_links:
    - from: ".github/workflows/security.yml"
      to: "GitHub Security Dashboard"
      via: "SARIF upload"
      pattern: "codeql-action/upload-sarif"
---

<objective>
Add security scanning to CI pipeline with gosec and govulncheck.

Purpose: Automatically detect security vulnerabilities in code and dependencies before they reach production. This meets SCAN-01 (SAST) and SCAN-02 (dependency scanning) requirements.

Output:
- GitHub Actions workflow for security scanning
- gosec for Go static analysis (fails on high severity)
- govulncheck for dependency vulnerabilities
- SARIF upload to GitHub security dashboard
</objective>

<execution_context>
@/Users/ahmedhamid/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmedhamid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-audit-infrastructure/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create security scanning workflow</name>
  <files>.github/workflows/security.yml</files>
  <action>
Create .github/workflows/security.yml:

```yaml
name: Security Scanning

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write  # Required for SARIF upload

jobs:
  gosec:
    name: Go Security Scan (SAST)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Run gosec
        uses: securego/gosec@master
        with:
          args: >
            -fmt sarif
            -out results.sarif
            -exclude-generated
            -severity high
            ./FastCRM/fastcrm/backend/...

      - name: Upload SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: results.sarif
          category: gosec

  govulncheck:
    name: Dependency Vulnerabilities
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./FastCRM/fastcrm/backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        run: govulncheck ./...
```

Key configuration choices:
1. `-severity high` - Only fail on high-severity findings (avoid false positive fatigue)
2. `-exclude-generated` - Skip generated code
3. `if: always()` for SARIF upload - Upload results even if scan finds issues
4. Separate jobs for gosec and govulncheck (can run in parallel)
5. Working directory set for govulncheck (go.mod location)

Create .github directory if it doesn't exist.
  </action>
  <verify>
File exists: `ls -la .github/workflows/security.yml`
YAML valid: `python -c "import yaml; yaml.safe_load(open('.github/workflows/security.yml'))"`
  </verify>
  <done>
Security scanning workflow created with gosec and govulncheck.
Will run on push to main and PRs.
High-severity findings will fail the build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create gosec configuration (optional)</name>
  <files>FastCRM/fastcrm/backend/.gosec</files>
  <action>
Optionally create a .gosec configuration file for fine-tuning:

```json
{
    "global": {
        "nosec": "enabled",
        "audit": "enabled"
    },
    "rules": {
        "G104": "true",
        "G304": "true",
        "G401": "true",
        "G501": "true"
    },
    "exclude-dir": [
        "vendor",
        "testdata"
    ]
}
```

This is OPTIONAL - the workflow will work without it.
Only create if you need to suppress specific false positives.

Common rules that may need tuning:
- G104: Errors unhandled (may flag fire-and-forget patterns)
- G304: File path provided as taint input (may flag file uploads)
- G401: Use of weak crypto (may flag old compatibility code)

For now, skip this task unless specific false positives appear.
  </action>
  <verify>
Skip if not created.
If created: `cat FastCRM/fastcrm/backend/.gosec`
  </verify>
  <done>
Optional: gosec configuration for false positive management.
Can be added later if false positives appear.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. .github/workflows/security.yml exists with valid YAML
2. Workflow includes gosec with SARIF output
3. Workflow includes govulncheck
4. Workflow triggers on push to main and PRs
5. Manual: Push to branch, verify workflow runs in GitHub Actions
</verification>

<success_criteria>
SCAN-01 satisfied: CI pipeline runs gosec and fails on high-severity findings.
SCAN-02 satisfied: Dependency vulnerability monitoring enabled via govulncheck.
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-infrastructure/10-06-SUMMARY.md`
</output>
